# for all new bookmarks
- ai enhance (existits today)
- add to pinboard (via API) (new gopher feature)
- to maintain the daily Popular stats (reason)

# Gopher Scheduled Task (every 24 hours)
- pull new bookmarks from Lumin API
- should already be ai enhanced
- add to pinboard (via API)
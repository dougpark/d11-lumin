# generate a new UI for token management

a small "API Tokens" section in the settings/user menu would be genuinely useful — just a list of your tokens with a "Create" button and a revoke ✕ per row. The raw token would appear once in a modal with a "copy" button, then never again.


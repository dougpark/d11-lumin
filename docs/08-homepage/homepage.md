# make a homepage view for a specific bookmark

# new homepage.html
- has the same style but is very simple
- just the bookmark title and description with a link to the bookmark URL

# url parameters
- the homepage view should be accessible via a URL like '/homepage/:bookmarkId'

# try to use the existing '/api/e/:dashboardTag' endpoint to fetch the bookmark data
- found in index.ts



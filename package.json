{
  "name": "d11-lumin",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create d11-db",
    "db:migrate:local": "wrangler d1 migrations apply d11-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply d11-db --remote",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^4.83.0"
  }
}
